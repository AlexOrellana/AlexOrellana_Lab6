#pragma once
#include<string>

using std::string;

class Juego{
	int id;
	string name;
	int year;
	string developers;
	string genre;
public:
	Juego(int,string,int,string,string);
	int getId();
	string toString()const;
};
